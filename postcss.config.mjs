// STEP: Tailwind/PostCSS configuration.
// Tailwind uses PostCSS to transform utility classes into CSS.
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
